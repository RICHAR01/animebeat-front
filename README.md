**AnimeBeat App**

**How to run app in production:**

Install dependencies: 
```
npm install
```


Go to file `/app/views/layout.server.view.html` and edit the API URLS (`API_URL, NEW_API_URL`)

Run command:
```
NODE_ENV=production node server.js
```

Requeriments:
```
node v8.x.x
```
